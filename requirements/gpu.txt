# Version verification date: 2026_04_28.
# Realistically only supported on Linux.
-r common.txt

# TF.
# tensorflow > 2.10 does not offer GPU support on Windows. However, lower versions are no longer available.
tensorflow[and-cuda]<=2.21.0; platform_system=="Linux"

# Pytorch. CUDA 12.6 necessary to support compute capability 6.1 (1080 Ti).
--extra-index-url https://download.pytorch.org/whl/cu126
torch<=2.11.0; platform_system!="Darwin"

# Jax.
# jaxlib, required by jax[cuda], does not support Windows.
-f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
jax[cuda12]<=0.10.0; platform_system=="Linux"
