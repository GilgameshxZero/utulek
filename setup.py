# Shared library for utulek Python code.
import setuptools

setuptools.setup(
    name="utulek",
    packages=["utulek"],
    # Ideally, this should be filled out, but I can't be bothered.
    install_requires=[],
)
